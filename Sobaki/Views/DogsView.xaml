<UserControl x:Class="Sobaki.Views.DogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sobaki.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="{StaticResource CreamColor}" Padding="10 0">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Поиск"/>
                    <TextBox Text="{Binding SearchText}" Width="150" Height="25" Margin="10 0"/>
                </StackPanel>
                <Button Content="По дате" Style="{StaticResource TextButtom}"/>

                <StackPanel Orientation="Vertical" Margin="10 0">
                    <TextBlock Text="Архив" HorizontalAlignment="Left"/>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Content="Смэрть"/>
                        <CheckBox Content="Забрали" Margin="10 0"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
            
            
        </Border>
    </Grid>
</UserControl>
