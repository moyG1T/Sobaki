<Page x:Class="Sobaki.Pages.EmployeesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Sobaki.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EmployeesPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="65*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Background="#C3A995">
            <Button Content="Добавить сотрудника" Margin="10"/>
            <Button Content="Редактировать" Margin="10"/>
            <StackPanel Margin="10" Width="100">
                <TextBlock Text="Поиск по ФИО" FontSize="14"/>
                <TextBox FontSize="14"/>
            </StackPanel>
            <StackPanel Margin="10" Width="150">
                <TextBlock Text="Поиск по должности" FontSize="14"/>
                <ComboBox Height="20"/>
            </StackPanel>
            <Button Content="Назад" HorizontalAlignment="Right" Margin="10"/>
        </DockPanel>

        <!--List-->

        <StackPanel Grid.Row="2" Orientation="Horizontal" Background="#C3A995"/>

    </Grid>
</Page>
